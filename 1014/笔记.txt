1、++num即num = num + 1;
2、~num(按位非)即-num - 1;
3、num += 10;即num = num + 10;
4、js中不存在块级作用越，for()中定义的变量在for()外部也可以访问。
5、label标签要求写在循环控制体的前面。
6、js中函数参数名不重要，可以通过arguments来取得参数值。
7、js中没有重载，同名函数后者覆盖前者。
8、js不允许直接访问内存中的位置。